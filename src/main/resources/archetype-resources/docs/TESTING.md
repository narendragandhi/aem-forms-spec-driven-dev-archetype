# Testing Standard

Run Java unit tests, frontend unit tests, browser journey tests, and accessibility
checks before calling a showcase complete. Fixtures must cover low-risk, review,
approval, rejection, timeout, duplicate submission, and degraded dependency paths.

Live AEM Author verification is required for authored form model JSON. Publish,
Adobe Sign, and Document of Record claims require separate environment evidence.
